# 문서 작성 규칙

이 문서는 Mavis가 한국어 요청으로 작성하는 모든 문서(DOCX, Markdown, 기획서, 보고서, 코드 주석 등)에 적용되는 규칙입니다.

---

## 규칙 1: 한문(Chinese characters / 漢字) 사용 절대 금지

### 왜 이 규칙이 필요한가
- 사용자는 한국어 문서를 원하지만, 웹 검색 결과나 LLM 학습 데이터에 중국어 한문이 자주 섞여 있음
- 項目(항목), 策略(전략), 報告(보고서) 같은 한자는 한국어 사용자에게 어색하고 읽기 불편함
- 사용자 명확한 피드백: "한문을 쓰지 말 것"

### 금지 대상
- 모든 한자 (CJK 한자 영역 U+4E00 ~ U+9FFF)
- 다음은 특히 자주 등장하므로 주의:
  - 項目 → 항목
  - 策略 → 전략
  - 報告 → 보고서
  - 創業 → 창업
  - 預測 → 예측
  - 評估 → 평가
  - 數據 → 데이터
  - 流量 → 트래픽
  - 渠道 → 채널
  - 規模 → 규모
  - 競爭 → 경쟁
  - 優勢 → 강점/우위
  - 趨勢 → 추세
  - 應用 → 응용/활용
  - 標準 → 표준
  - 結構 → 구조
  - 創新 → 혁신
  - 場景 → 시나리오
  - 用戶 → 사용자
  - 產品 → 제품
  - 服務 → 서비스
  - 內容 → 내용
  - 問題 → 문제
  - 解決 → 해결
  - 分析 → 분석
  - 目標 → 목표
  - 發展 → 발전
  - 計劃 → 계획
  - 風險 → 리스크
  - 機會 → 기회
  - 收益 → 수익
  - 成本 → 비용
  - 技術 → 기술
  - 商業 → 비즈니스/상업
  - 市場 → 시장
  - 客戶 → 고객
  - 行業 → 산업
  - 領域 → 영역
  - 環境 → 환경
  - 系統 → 시스템
  - 平台 → 플랫폼
  - 模式 → 모델
  - 互聯網 → 인터넷
  - 人工智能 → 인공지능
  - 數字 → 디지털
  - 自動化 → 자동화
  - 測試 → 테스트
  - 開發 → 개발
  - 設計 → 설계
  - 部署 → 배포
  - 運營 → 운영
  - 維護 → 유지보수
  - 升級 → 업그레이드
  - 優化 → 최적화
  - 推廣 → 홍보
  - 銷售 → 판매
  - 採購 → 구매
  - 物流 → 물류
  - 金融 → 금융
  - 醫療 → 의료
  - 教育 → 교육
  - 製造 → 제조
  - 批發 → 도매
  - 零售 → 소매
  - 消費 → 소비
  - 投資 → 투자
  - 融資 → 자금조달
  - 上市 → 상장
  - 創業 → 창업
  - 變革 → 변혁
  - 突破 → 돌파
  - 創意 → 아이디어

### 변환 방법
1. **한자음 표기** (예: 創新 → 창신) — 한자 발음을 한국어로 적는 방식. 대부분 어색하니 비추천.
2. **한국어 번역** (예: 創新 → 혁신) — 의미를 한국어로 풀어쓰는 방식. **권장**.
3. **영어 단어 차용** (예: 流量 → 트래픽, 渠道 → 채널) — 업계 표준 용어가 있는 경우.

### 예외 (허용되는 한자)
- 고유명사/브랜드명 (예: 微信(WeChat), 支付宝(Alipay), 知乎(Zhihu), 小红书(Xiaohongshu), 抖音(Douyin))
- 인명, 지명 등 고유명사에 포함된 한자
- 한국 고유 한자어(예: 한옥, 한복, 한글)는 한자가 아니라 한국어 단어이므로 OK

### 위반 시 처리
- 즉시 검수하여 한자 발견 시 한국어로 변환
- 재출력 전 다시 검증
- 검증 코드: `re.compile(r'[　-鿿＀-￯]')` 로 한자 영역 매칭 확인

### 적용 범위
- DOCX 본문, 표, 목록, 제목
- Markdown 파일 본문
- JSON 콘텐츠 파일
- 코드 주석 (한국어 부분)
- 이메일, 메신저 응답의 한국어 부분
- 슬랙/노션/지라 등 모든 산출물

---

## 부록: 검증 스크립트

문서 작성 후 한자 검증을 위해 다음 Python 코드를 실행:

```python
import json, re

# JSON 콘텐츠 파일인 경우
data = json.load(open('content.json', encoding='utf-8'))
chinese_pattern = re.compile(r'[　-鿿＀-￯]')

found = False
for i, item in enumerate(data):
    text = item.get('text', '')
    if chinese_pattern.search(text):
        print(f'Item {i}: {text[:80]}... | 한문 발견')
        found = True

if not found:
    print(f'한문 없음! 총 {len(data)}개 아이템 모두 깨끗함')
```

```python
# 일반 텍스트 파일인 경우
text = open('document.md', encoding='utf-8').read()
chinese_pattern = re.compile(r'[　-鿿＀-￯]')
matches = chinese_pattern.findall(text)
if matches:
    print(f'한문 {len(matches)}개 발견: {set(matches)}')
else:
    print('한문 없음 - 깨끗함')
```

---

## 변경 이력
- 2026-06-23: 최초 작성 (사용자 피드백 기반)
